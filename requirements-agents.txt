# Agent系统专用依赖
# 建议在项目虚拟环境中安装

# CrewAI Agent框架
crewai>=0.35.0
# 注意：如果版本冲突，可以尝试更早期的稳定版本
# crewai==0.30.0

# 兼容性依赖
langchain>=0.1.0,<0.2.0
langchain-openai>=0.0.2,<0.1.0
langchain-community>=0.0.20,<0.1.0

# 基础依赖 (与主项目共用)
pydantic>=2.0.0
python-dotenv>=1.0.0
openai>=1.7.0

# 可选：如果需要更多工具
# beautifulsoup4  # 网页解析
# requests>=2.28.0  # HTTP请求